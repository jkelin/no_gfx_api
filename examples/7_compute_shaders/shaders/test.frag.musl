Data :: struct
{
    texture_id: textureid,
    sampler_id: samplerid,
}

main :: (uv: vec2 @in_loc(0), data: ^Data @data) -> vec4 @out_loc(0)
{
    return sample(data.texture_id, data.sampler_id, uv);
}
