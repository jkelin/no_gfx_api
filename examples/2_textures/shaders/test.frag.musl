
Data :: struct
{
    texture_a: textureid,
    texture_b: textureid,
    s: samplerid,
    fade: float,
}

main :: (uv: vec2 @in_loc(0), data: ^Data @data) -> vec4 @out_loc(0)
{
    return mix(sample(data.texture_a, data.s, uv), sample(data.texture_b, data.s, uv), data.fade);
}
